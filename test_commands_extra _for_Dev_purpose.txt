CleaUp commands-
rm -rf backend/modules/*/__pycache__
rm -rf backend/modules/__pycache__
rm -rf desktop/__pycache__
rm -rf backend/__pycache__
rm -rf /home/$USER/.local/share/lytrize
rm -rf build

Get-ChildItem backend\modules -Recurse -Directory -Filter __pycache__ | Remove-Item -Recurse -Force -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force backend\modules\__pycache__ -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force desktop\__pycache__ -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force backend\__pycache__ -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force "$env:APPDATA\Lytrize" -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force build -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force "$env:LOCALAPPDATA\Lytrize" -ErrorAction SilentlyContinue


cd sponsorscout
python -m venv venv
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
.\venv\Scripts\Activate.ps1
python desktop/gui.py

python career_portal_scanner_v6.py

To see only the libraries installed in this specific portable folder, run:
.\python-3.10.x.amd64\python.exe -m pip list

To delete a specific library that you do not require, 
use the -m pip uninstall command alongside the -y flag (so it deletes automatically without asking you to type "yes"):
.\python-3.10.x.amd64\python.exe -m pip uninstall <library_name> -y
OR 
Inside WinPython Command Prompt.exe window, you can simply type standard commands:
pip list
pip uninstall <library_name> -y

python desktop/gui.py


python3 -m streamlit run backend/app.py
python3 -m streamlit run desktop/gui.py

python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 -m streamlit run backend/app.py
sudo dnf install rpm-build -y
chmod +x build_rpm.sh
./build_rpm.sh

powershell -ExecutionPolicy Bypass -File build_windows.ps1


Git commands - 
git init
git branch -M main
git remote add origin https://github.com/Kazake95/lytrize_desktop.git
git add .
git commit -m "Initial commit"
git push -u origin main

git config --global user.email "bantybnath.com"
git config --global user.name "Kazake95"

Check & analyse my codebase of BI tool  for linux. I want a windows port of this linux only verion so i need a windows setup installer build script for powershell with installer install the app in system directory , saving data file in appdata directory & uninstalling app would remove everything from pc. I use  inno setup 7 for installer framework located inside - "C:\Program Files\Inno Setup 7".
Also, for launcher GUI , linux path are hardcoded, thus browser selection won't work for windows. You need to add windows path as well which app can in windows pc.

Some other things - 
1. I don't want any extra python shell or window open up while lauching app in windows pc. Only launcher GUI with browser select & actual isolated selected browser windows should be open for actual app GUI. The laucnher GUI should be minimize to tray when click to taskbar icon is click.
2.when "Stop & Quit" button click in launcher GUI , it  should also close the isolated browser windows of app as well with it, because even after process stopped & quited, browser window still got opened.

Make phase-wise plan after clear analysis no monkey patch.